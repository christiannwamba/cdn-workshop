# Browser controls — observed 2026-09-25

- Manual Chrome session `run-75a2c105-322e-47fc-a817-6761adb4577b`: A MISS, B/Missing/Invalid HIT, all joins passed. Two further A sends were HIT, with one origin and six total native records/emissions. Browser receipts and genuine redacted records are saved separately.
- An immediate retry of a consumed ticket returned **409**, with no response event UUID or cache header. A deliberately reserved one-second ticket sent after expiry returned **403**, with no event UUID/cache header. Rejections did not add a content-origin call or successful cookie emission.
- The actual ten-minute session expired during testing. Its UI displayed “Session expired”; Send received **410** from the session API while earlier evidence remained readable. Fresh per-request tickets had allowed pauses between successful requests beforehand.
- Browser Run all in the in-app browser created `run-f2dc30a5-9475-455a-a244-533217de048c`: four real fetches, MISS/HIT/HIT/HIT, one origin, four emissions/native records. All four states verified.
- The same run was joined from Chrome, using a revealed temporary key transferred privately through the lab UI. Chrome set B and received HIT; the in-app browser retained A and received HIT afterward. Final count: one origin, six native records, six emissions, six joins. This establishes separate cookie jars on one machine, not separate people or all geographic cache topologies.
- Reset in Chrome revoked that shared session; the in-app browser's next Send returned **410**. Reset clears the test cookies in its own jar; the other context also used Reset to clear its cookies.
- A valid key from that earlier run was offered to a different registered run; Join returned **401**. Keys were never put in URLs, recorded receipts or committed evidence.
- Out-of-order session `run-640438ba-8011-41ea-80b3-f11c20cf6178`: B first was MISS/PASS; A next was HIT/PASS. UI stayed **PARTIAL · B, A verified**, with one origin and no false failed/completed-suite claim.
- During an earlier deployment transition, a session from the previous request deployment received a useful **409 deployment changed** response. Reset/reload recovered with a fresh session.
- Additional HTTP protocol controls (not browser transport proof) are in `boundaries.json`: direct-origin use of a browser ticket rejected before and after consumption, private upstream authorization absent from response headers, wrong capabilities rejected, the thirteenth allocated ticket rejected, and reset/cross-environment boundaries enforced.

Browser receipts include actual fetch responses and browser ResourceTiming. Chrome revalidated cached bodies (`deliveryType=cache`, 300 bytes transferred for fresh headers); each accepted fetch nevertheless has a new delivered UUID/native request ID. The DevTools Network recorder was enabled with Disable cache unchecked, but no raw HAR was published because it can contain temporary tickets or unrelated browser data. The browser response/ResourceTiming capture plus genuine signed emissions supplies the transport evidence.
